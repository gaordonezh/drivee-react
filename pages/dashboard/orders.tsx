import Layout from '@/components/templates';
import LayoutEnum from '@/enums/layout.enum';
import React from 'react';

const Orders = () => {
  return <Layout layout={LayoutEnum.DASHBOARD}>Orders</Layout>;
};

export default Orders;
